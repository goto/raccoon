name: Raccoon Integration Test

on:
  - push
  - pull_request

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Setup Docker
        uses: docker-practice/actions-setup-docker@master
      - name: Checkout repo
        uses: actions/checkout@v2
      - name: Copy integration config
        run: cp .env.test .env
      - name: Run Raccoon
        run: make docker-run
      - name: Docker Testing
        run: docker ps -a
      - name: Conatainer Logs
        run: docker logs $(docker ps -aqf "name=cs")
      - name: Testing
        run: echo $INTEGTEST_BOOTSTRAP_SERVER $INTEGTEST_HOST $INTEGTEST_TOPIC_FORMAT $GRPC_SERVER_ADDR
      - name: Invoking go test
        run: INTEGTEST_BOOTSTRAP_SERVER=localhost:9094 INTEGTEST_HOST=localhost:8080 INTEGTEST_TOPIC_FORMAT="clickstream-%s-log" GRPC_SERVER_ADDR="localhost:8081" go test ./integration -v
